#include <stdio.h>
#include<math.h>
int main() 
{
  long long b;
  int i=0,y,sum=0;
  printf("Enter the binary\n");
  scanf("%lld",&b);
  while(b!=0)
  {
      y=b%2;
      sum=sum+y*pow(2,i);
      b=b/10;
      i++;
  }
  printf("decimal is %d",sum);

    return 0;
}
