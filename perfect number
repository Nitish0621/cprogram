#include <stdio.h>
int main()
{int n,i,x,sum=0;
printf("Enter the number\n");
scanf("%d",&n);
for(i=1;i < n;i++)
 {
  x=n%i;
  if(x==0)
  sum=sum+i;
 }
  if(sum==n)
  printf(" %d is a perfect number",n);
  else
  printf("%d is not a perfect number",n);
 return 0;
}
