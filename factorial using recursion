#include <stdio.h>
int fact(int);
int main()
{
int num;
printf("Enter the number\n");
scanf("%d",&num);
int f=fact(num);
printf("%d",f);

    return 0;
}
int fact(int a)
{
    int x;
    if(a==0)
       return 1;
    else
       return(a*fact(a-1));
   
}
