
#include <stdio.h>
void fact(int);
int main()
{
int num;
printf("Enter the number\n");
scanf("%d",&num);
fact(num);

    return 0;
}
void fact(int a)
{
    int x;
    if(a==0)
    return 1;
    
    x=a*fact(a-1);
    printf("%d",x);
}
