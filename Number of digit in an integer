#include <stdio.h>

int main() {
    int num,i=0;
printf("Enter the integer\n");
scanf("%d",&num);
while(num>0)
   {
       i++;
       num=num/10;
   }
   printf("number of digit in integer is %d",i);
    return 0;
}
