#include <stdio.h>
int main(){
 int a[10],i,max;
 printf(" Enter Ten Number");
 for(i=0;i<=9;i++)
   scanf("%d",&a[i]);
   max=a[0];
   for(i=1;i<=9;i++){
       if(max>a[i])
          max=a[i];
 }
    printf("Smallest Element is %d",max);
 return 0;
}
